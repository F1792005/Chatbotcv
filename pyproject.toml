# pyproject.toml
[project]
name = "chatbotcv"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
    # Nếu dùng Gemini:
    "google-generativeai"
]

[tool.uv]
# Không cần gì thêm